server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: hot_list
  cloud:
    nacos:
      config:
        server-addr: 82.157.62.176:18848
        file-extension: yml

  profiles:
    active: nacos